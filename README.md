Simpliest_Windows_TouchScreen_Toggle


You just need to execute `script.ps1` in an administrator PowerShell ans magic happend !

In the next version, the script can be run without administratif rights, in thé background ans with a simple keyboard shortcut.